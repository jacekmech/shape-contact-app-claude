# Base URL for API requests.
# Leave empty (default) to use relative paths routed through nginx.
# Set to an absolute URL only when running the frontend standalone without nginx.
# Default: '' (empty string — relative path)
VITE_API_BASE_URL=
